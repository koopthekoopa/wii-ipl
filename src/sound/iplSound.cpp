#include "iplSound.h"
#include "sound/iplSound.h"

namespace ipl {
    namespace snd {
        void System::shutup(BOOL shutUpDMA) {
            // if (ipl::snd::is)
        }
    }  // namespace snd
}  // namespace ipl
