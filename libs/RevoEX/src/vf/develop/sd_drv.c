// wrok :sob:
void VFi_InitSDWrok() {
    // Stubbed out because the version it uses strips out SD card support.
    // Actually, I don't think you were normally supposed to use VF for SD Card support, and instead you were supposed to use the FA library for that
    // (similar to VF but bigger and without .vff support)

    // I think only a couple of games use VF for SD Card support.

    // The Wii Menu, despite using FA for SD Card support, also uses VF for SD Card for the CDB library (Wii Message Board data)
}
